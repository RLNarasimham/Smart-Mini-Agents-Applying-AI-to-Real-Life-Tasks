name: Trigger Vercel Deploy Hook for Nava-AI
on:
  push:
    branches: [ "Nava-AI" ]
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: curl -s -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_NAVA_AI }}"
